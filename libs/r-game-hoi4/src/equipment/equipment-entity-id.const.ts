// The entity-identifier constant shared by the read-side equipment recognizer
// and the write-side plane form descriptor (ADR 018) — one constant keeps the
// two registries keyed identically and from drifting.
export const EQUIPMENT_ENTITY_ID = 'hoi4-equipment';
