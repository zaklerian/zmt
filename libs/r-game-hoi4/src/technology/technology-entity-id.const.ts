// The entity-identifier constant shared by the read-side technology recognizer
// and the write-side technology form descriptor (ADR 018) — one constant keeps
// the two registries keyed identically and from drifting.
export const TECHNOLOGY_ENTITY_ID = 'hoi4-technology';
