// The entity-identifier constant shared by the read-side module recognizer and
// the write-side module form descriptor (ADR 018) — one constant keeps the two
// registries keyed identically and from drifting.
export const MODULE_ENTITY_ID = 'hoi4-module';
