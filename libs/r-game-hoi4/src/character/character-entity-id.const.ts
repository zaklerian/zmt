// The entity-identifier constant shared by the (future) read-side character
// recognizer and the write-side character form descriptor (ADR 018) — one
// constant keeps the two registries keyed identically and from drifting.
export const CHARACTER_ENTITY_ID = 'hoi4-character';
