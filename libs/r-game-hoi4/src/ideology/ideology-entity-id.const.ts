// The entity-identifier constant shared by the read-side ideology recognizer and
// the write-side ideology form descriptor (ADR 018) — one constant keeps the two
// registries keyed identically and from drifting.
export const IDEOLOGY_ENTITY_ID = 'hoi4-ideology';
